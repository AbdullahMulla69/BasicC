#include <stdio.h>
main()
{
    int year;
    printf("Enter current year= ");
    scanf("%d",&year);
    if (year%4==0)
    {
      printf("It is Leap Year");
    }
    else
    {
        printf("It is NOT Leap Year");
    }
}
