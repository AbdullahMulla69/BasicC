#include<stdio.h>
int fact(int n)
{
    if(n==1)
    {
        return 1;
    }
    else
    {
        return n*fact(n-1);
    }
}
void main()
{
    int num;
    printf("Number: ");
    scanf("%d",&num);
    int result=fact(num);
    printf("Factorial is: %d",result);
}
