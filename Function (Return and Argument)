#include<stdio.h>
float aoc(int r)
{
    float area;
    area=3.14*(r*r);
    return area;
}
main()
{
    int radius;
    printf("Enter radius: ");
    scanf("%d",&radius);
    float a=aoc(radius);
    printf("Area of circle is: %0.2f",a);

}
