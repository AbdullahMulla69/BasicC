#include <stdio.h>
main()
{
    int a;
    printf("Enter a number= ");
    scanf("%d", &a);
    if (a!=0)
    {
        if (a>0)
        {
            printf("Number is Greater");
        }
        else
        {
            printf("Number is Smaller");
        }
    }
    else
    {
        printf("Number is Equal");
    }
}
