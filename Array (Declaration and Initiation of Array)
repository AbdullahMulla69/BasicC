#include<stdio.h>
main()
{
    int a[4]; 
    int i;
    for(i=0;i<=3;i++)
    {
        printf("Enter %d roll no.=",i+1);
        scanf("%d",&a[i]);
    }
    for( i=0;i<=3;i++)
    {
        printf("Student %d is",i+1);
        printf("%d",a[i]);
        printf("\n");
    }
}
